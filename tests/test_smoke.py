"""Smoke test — confirms the test harness is wired up.

Replace with real tests as the extract/load/transform layers land
(e.g. dedup logic, schema assertions). Run with: pytest
"""


def test_placeholder():
    assert True
