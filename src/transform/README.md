# transform

Phase 3: SQL / dbt models that reshape `raw` into the `analytics` star schema
(`dim_track`, `dim_artist`, `fact_plays`). `dbt init` will populate this
directory; until then it holds the transformation logic stubs.
